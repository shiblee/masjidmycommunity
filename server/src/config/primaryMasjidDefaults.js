// How long a user who skipped the Primary Masjid picker goes before it's
// shown again. A single named constant for now — the spec calls this
// "configurable from admin side in the future," so this is the one place
// that future setting would replace.
export const PRIMARY_MASJID_REMINDER_DAYS = 7;

// How many nearby masjids the picker shows at once.
export const NEARBY_MASJID_LIMIT = 12;
